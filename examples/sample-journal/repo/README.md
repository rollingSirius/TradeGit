# Sample Trading Journal

Fictional TradeGit journal used for demos and tests.

The journal is local-only: it has no remote and should not contain real account
data. To create your own local journal, run:

```bash
tradegit init --local
```
